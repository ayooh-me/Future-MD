import fetch from 'node-fetch'
let handler = async(m, { conn, usedPrefix, text, command }) => {
throw "FITUR BELUM DI TAMBAHKAN"
}
handler.command = handler.help = ['nothing']
handler.tags = ['tools']

export default handler